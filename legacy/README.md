# Previous React / Vinext implementation

Preserved as a reference during migration to Astro. These files are not compiled,
deployed, or dependencies of the current site. The previous package manifest is
retained for context, not as a second active project. All 12 original photographs
remain in `public/products/`, and the original technical PDF remains in public.

The active implementation is in `src/`. Product galleries now use native HTML
disclosures instead of the React Slides/Grid controls.
